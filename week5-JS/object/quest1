class MyPet {
    constructor(name, type) {
        this.name = name;
        this.type = type;
    }

    show() {
        console.log(`name: ${this.name}, type: ${this.type}`);
    }
}

const pet1 = new MyPet('MoMo', 'cat');
pet1.show();